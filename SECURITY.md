# Security and sensitive data

Do not open issues containing Microsoft/Xbox tokens, Wine prefixes, package
files, decrypted executables, licensing data, or private build links.
Revoke exposed sessions through your Microsoft account immediately.

This repository intentionally ignores common package/game formats and runtime
profile directories. Review every commit before pushing.
